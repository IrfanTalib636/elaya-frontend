/** Default page size for studio list pages (matches backend default). */
export const PAGE_SIZE = 20

/** Max cards shown per pipeline column before "Mehr anzeigen". */
export const CRM_KANBAN_COLUMN_LIMIT = 15

/** Max fetch when client-side search is active (Cases/Sessions). */
export const SEARCH_FETCH_LIMIT = 100
